document.addEventListener('DOMContentLoaded', () => {
  // var canvas = document.querySelector('.tip-triangle');
  //
  // console.log(canvas)
  //
  // var context = canvas.getContext('2d');
  //
  // context.beginPath();
  // context.moveTo(2, 2);
  // context.lineTo(22, 22);
  // context.lineTo(2, 22);
  // context.closePath();
  // context.strokeStyle = '#dcdcdf';
  // context.stroke();
  //
  // // context.fillStyle = "rgb(78, 193, 243)";
  // context.fillStyle = "#f7f7f7";
  // context.shadowColor = 'rgba(60, 75, 100, 1)';
  // context.shadowBlur = 0;
  // context.shadowOffsetX = 1;
  // context.shadowOffsetY = 1;
  // context.fill();

});
